services:
  - type: web
    name: snake-ladder-server
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: ~18.18.0
